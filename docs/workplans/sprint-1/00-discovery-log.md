> Status: Discovery evidence — not an approved implementation contract.
>
> This document describes the repository state inspected at the recorded Git
> commit. Proposed Sprint 1 behavior remains subject to review and approval.

# Discovery log

All timestamps are UTC. Commands are reproduced in sanitized form; no URL, token, IP address,
personal player data, or raw diagnostic evidence is included. Only Sprint 1 documentation files
were intentionally created or modified; no runtime files were changed.

Time-recording correction: original discovery entries record known dates but did not capture exact
UTC times; those historical entries are preserved rather than reconstructed. This correction pass
uses full UTC timestamps for new entries.

| # | Time | Action / purpose | Evidence inspected / command | Result | Classification | Files changed | Question / next action |
|---:|---|---|---|---|---|---|---|
| 1 | 2026-08-27 | Read task and mandatory onboarding | pasted brief; `AGENTS.md`; all five `docs/handoff/*.md` | Discovery-only scope and evidence guardrails confirmed. | Documented decision | none | Inspect contracts, ADRs, runbooks and source. |
| 2 | 2026-08-27 | Establish repository baseline | `Get-ChildItem -Force`; `git status --short`; `git branch --show-current`; `git rev-parse HEAD`; `git remote -v`; `rg --files …` | Clean start; branch and commit recorded in README. `docs/vision` absent. Remote was listed without credentials. | Implemented and production-wired | none | Trace public API, queue and child boundary. |
| 3 | 2026-08-27 | Read governing decisions and operations | `Get-Content -Raw docs/decisions/*.md docs/contracts/*.md docs/runbooks/*.md .github/workflows/*.yml` | ADRs distinguish current code from future durability; CI runs no production inference; deploy is main-only after CI. | Documented decision | none | Compare with current code/tests. |
| 4 | 2026-08-27 | Required symbol discovery | `rg -n -S "playerId|player_id|track_id|target|selector|overall|…|callback" src tests docs` | Required concepts occur in API, pipeline, selectors, rating engines, mapper and focused tests. Output was truncated, so focused symbol/file reads followed. | Implemented and production-wired | none | Inspect current composition, selectors and formulas. |
| 5 | 2026-08-27 | Trace implementation ownership | Focused `rg` plus source reads for schemas, routes, queue, child, tracker, selection, segment selection, rating engines, mapper, callback, configuration and focused tests | `playerId` stays request/callback identity; selectors use visual-track evidence only; ratings are provisional and gated, but target eligibility is not a rating gate. | Implemented and production-wired | none | Draft discovery documents and run safe focused tests. |
| 6 | 2026-08-27 | Create required discovery pack | `apply_patch` | Added only `docs/workplans/sprint-1/*`; no runtime files changed. | Proposed | this directory | Verify links, diff, and focused tests. |
| 7 | 2026-08-27 | Run focused deterministic tests | `uv run pytest -q <selector/rating/contract/processor tests>`; direct `.venv` pytest fallback | Neither launcher reached pytest: local cache/interpreter child creation failed with permission errors. No test result is claimed. | Unknown / requires verification | none | Re-run in an environment where the project Python launcher may create child processes. |
| 8 | 2026-08-27 | Verify documentation diff and links | `git diff --check`; PowerShell repository-relative Markdown-link check; `git status --short` | No `git diff --check` errors; link check resolved every repository-relative discovery-doc link. Git reports only untracked `docs/workplans/`. `git diff --check` does not inspect untracked files. | Implemented but not production-wired | none | Human/review-tool Markdown rendering still advisable. |
| 9 | 2026-08-27 | Documentation-correction provenance check and correction | `git rev-parse HEAD`; `git diff --name-status 7920375… f2d1e834…`; `git log -1 f2d1e834…`; continuation brief | Current HEAD is documentation commit `f2d1e834…`; range contains only the eight Sprint 1 Markdown files. Per continuation brief, it was committed and pushed after the original discovery work. Local evidence verifies the commit/range, not remote push receipt. | Implemented but not production-wired | Sprint 1 Markdown only | Correct provenance, traceability, rating audit and decision register. |
| 10 | 2026-08-27 | Verify correction documentation | `git diff --check`; PowerShell repository-relative Markdown-link check; targeted terminology search; `git status --short` | No whitespace errors; all repository-relative Sprint 1 links resolve. Git reports only the eight modified Sprint 1 Markdown files. Git emitted LF-to-CRLF warnings for these working-copy files; no content error was reported. | Implemented but not production-wired | Sprint 1 Markdown only | Correction complete; approval decisions remain blocked. |
| 11 | 2026-08-27 | Validate detailed-rating and Overall symbol references | `Get-Content -Raw src/services/detailed_rating/engine.py src/services/player_rating/engine.py` | Confirmed `DetailedRatingEngine._event_score`, `_ball_control`, `_visible_movement_activity`, and `PlayerRatingEngine._overall` references used by the expanded rating audit. | Implemented and production-wired | none | No runtime change; documentation references are verified. |
| 12 | 2026-08-27T19:24:32.0386968Z | Capture correction-pass Git evidence and initial status | `git status --short`; `git show --stat --summary f2d1e834…`; baseline-to-documentation `git diff --stat` and `--name-status` | Initial correction tree has exactly eight modified Sprint 1 Markdown files. Commit/range show eight documentation-only additions (325 insertions); no runtime behavior changed between baseline and documentation commit. | Implemented but not production-wired | none | Add hierarchy, future metrics and truthful final verification. |
| 13 | 2026-08-27T19:25:31.4048622Z | Inspect every modified discovery document and run correction checks | Read all eight Sprint 1 Markdown files; `git diff --check`; `rg` trailing-whitespace check; link checker; scope/status/stat check; search for Markdown tooling | All eight inspected. No `git diff --check` content error, no trailing whitespace, and all repository-relative links resolve. Only eight Sprint 1 Markdown files are modified. Git emitted LF-to-CRLF warnings. No repository Markdown-specific checker was found. No tests reattempted; original environment limitation remains. | Implemented but not production-wired | none | Final documentation summary only. |
| 14 | 2026-08-27T19:26:13.3951438Z | Final correction validation | `git diff --check`; trailing-whitespace scan; repository-relative link check; scope check; `git diff --stat`; `git status --short` | No diff-check content error, trailing whitespace, broken repository-relative links, or out-of-scope changes. Eight Sprint 1 Markdown files only; captured stat was 246 insertions and 76 deletions. LF-to-CRLF warnings remain informational. | Implemented but not production-wired | none | Human and Apex review remains the next permitted step. |

## Commands and checks

| Command (sanitized) | Purpose | Result / exit | Changed files? |
|---|---|---|---|
| `Get-Content -Raw <brief and handoff files>` | Read task and canonical handoff | completed / 0; output truncation handled by later reads | no |
| `git status --short; git branch --show-current; git rev-parse HEAD; git remote -v` | Baseline state | completed / 0; clean, branch/commit recorded | no |
| `rg --files …` | Inventory source, tests and governing documentation | completed / 1 because `docs/vision` is absent | no |
| `Get-Content -Raw <ADRs/contracts/runbooks/workflows>` | Read governing material | completed / 0 | no |
| `rg -n -S <required terms> src tests docs` | Required reference search | completed / 0; output truncated, not treated as exhaustive proof | no |
| focused `rg` / source reads | Definition and test trace | completed / 0 | no |
| `uv run pytest -q <focused tests>` | Safe behavioral verification | not executed: UV cache/interpreter permission error before pytest startup | no |
| `.venv\\Scripts\\python.exe -m pytest -q <focused tests>` | Fallback behavioral verification | not executed: UV trampoline permission error before pytest startup | no |
| `git diff --check` | Whitespace inspection | completed / 0; applies to tracked diff only, hence excludes newly untracked files | no |
| PowerShell Markdown-link check | Verify discovery-doc repository-relative links | completed / 0; all resolved | no |
| `git diff --name-status 7920375… f2d1e834…` | Verify baseline-to-documentation range | completed / 0; eight Sprint 1 Markdown additions only | no |
| `git diff --check`; link/terminology checks | Validate correction documentation | completed / 0; links resolve, only Sprint 1 Markdown modified; LF-to-CRLF warnings observed | no |
| `Get-Content -Raw <detailed/player rating engines>` | Validate audit symbol references | completed / 0 | no |
| `git show --stat --summary f2d1e834…`; baseline-range diff stat/name-status | Capture required provenance evidence | completed / 0; documentation-only additions | no |
| read-all-documents; diff/whitespace/link/scope/Markdown-tool checks | Final correction verification | completed / 0; all static checks pass, LF-to-CRLF warnings; no Markdown checker available | no |
| final diff/whitespace/link/scope/stat/status checks | Final correction validation | completed / 0; all checks pass; only Sprint 1 Markdown modified | no |

Assumptions avoided: `playerId == track_id`; event confidence equals skill; pixels equal metres or
fitness; documentation proves runtime behavior; track continuity proves initial identity; null is
zero; and current scores are calibrated. The original discovery session performed no inference,
live request, deployment, configuration mutation, package install, model download, commit or push.
Afterward, the documentation was committed as `f2d1e834…` and, per this correction request, pushed;
this correction does not rewrite that history.

## Contract approval documentation activity

The original discovery entries retain their historical timestamp limitations. The task brief read
occurred before a tool-provided UTC timestamp was captured and is recorded as such rather than
inventing a time. Subsequent entries use captured full UTC timestamps.

| UTC timestamp | Action | Purpose | Files/symbols inspected | Command | Result | Classification | Files changed | Decision affected | Unresolved question | Next action |
|---|---|---|---|---|---|---|---|---|---|---|
| not captured | Read contract-approval brief | Establish documentation-only scope and approved product facts. | User-provided brief | `Get-Content -Raw <attachment>` | Brief supplied dominant-candidate contract requirements. | Documented decision | none | D1–D15 | Apex API confirmation remains pending. | Complete onboarding/source verification. |
| 2026-08-29T12:09:36.5374540Z | Inspect selector and relevant rating flow | Verify contract feasibility and current selector limits. | `segment_selection.py:build_segments,rank_segments,select_segment`; `selection.py:WeightedTargetPlayerSelector`; route/rating mappers; focused tests | `Get-Content -Raw <source/tests>`; `git log`; baseline diff | Default segment mode sorts composite quality and selects first; no runner-up dominance/status/input-guarantee binding. | Implemented and production-wired | none | D1, D2, D3, D6, D12, D13 | Operational dominance rule/threshold unknown. | Create ADR and proposed behavioral contract. |
| 2026-08-29T12:09:36.5374540Z | Create proposed contract artifacts | Formalize approved product semantics without runtime change. | Discovery pack and prior ADRs | `apply_patch` | Added `ADR-005-dominant-visual-target-mvp.md` and `target-selection-contract-v1.md`. | Proposed | ADR and contract | D1–D14 | Apex public/API confirmation pending. | Align discovery documents. |
| 2026-08-29T12:09:36.5374540Z | Align discovery documentation | Correct pipeline and record semantic decisions. | Sprint 1 README, target/identity, ratings, decision register, implementation map | `apply_patch` | Added documented-decision sections, approved semantic addendum, and automatic-flow order. | Documented decision | Sprint 1 Markdown | D1–D15 | Thresholds and public shape unresolved. | Static verification and final record. |
| 2026-08-29T12:12:33.7075214Z | Run contract-documentation static checks | Verify scope, links, whitespace, Git state and Markdown tooling. | New ADR/contract and modified workplan files | `git status`; `git diff --stat`; `git diff --check`; `rg`; PowerShell link checker | Branch/HEAD `the-new-inhancement`/`f8664cd…`; only intended Markdown files changed; diff check clean; no trailing whitespace; links resolve; no Markdown tooling found. | Implemented but not production-wired | none | D1–D15 | Tests remain unexecuted; thresholds/API pending. | Update verification record. |
| 2026-08-29T12:14:03.4634315Z | Recheck corrected hierarchy and final static state | Confirm automatic ordering, conceptual-language safety, whitespace, links and diff scope. | ADR, contract, all changed Sprint 1 documents | `rg`; `git status`; `git diff --check`; whitespace/link check; `git diff --stat` | No stale automatic target-before-tracking order found; required caveats remain present; no diff-check/trailing-whitespace/link error. Seven modified Sprint 1 docs plus two new Markdown docs are the only changes. | Implemented but not production-wired | none | D1–D15 | Apex/API confirmation and thresholds remain unresolved. | Final response. |
| 2026-08-29T12:31:34.2179232Z | Inspect dominance wording for hotfix | Locate unsafe single-qualifier contract language without runtime discovery. | ADR-005, target-selection contract, Sprint 1 workplans | `rg -n -i -S <dominance terms>` | Found `yes by definition` in contract truth table and related dominance references; no runtime files inspected or changed. | Documented decision | none | Dominance policy | Plausibility/dominance operational definitions unresolved. | Replace unsafe wording and update mandatory records. |
| 2026-08-29T12:31:34.2179232Z | Apply dominance documentation hotfix | Separate qualification from plausible-alternative/dominance behavior. | ADR-005, contract, decision register, implementation map, README | `apply_patch` | Replaced unsafe truth-table row; documented no unresolved plausible alternative requirement and future policy split. | Documented decision / Proposed contract behavior | Markdown only | Dominance policy | Thresholds remain unresolved. | Run static verification. |
| 2026-08-29T12:32:28.3474040Z | Verify dominance hotfix | Search unsafe terms; verify diff, whitespace, links, status and stat. | All Sprint 1 docs, ADR-005, target-selection contract | `rg`; `git diff --check`; whitespace/link checks; `git status`; `git diff --stat` | No `yes by definition` or automatic establishment remains. Remaining single-qualifier text explicitly prohibits it. No diff-check/trailing-whitespace/link error; six Markdown docs only. | Documented decision / Proposed contract behavior | none | Dominance policy | Numeric qualification/plausibility/dominance policies remain unresolved. | Update verification record and hand off review. |
| 2026-08-29T12:49:03.1336623Z | Inspect candidate, process, and rating boundaries | Determine minimal dominant-target implementation design without changing code. | Tracker, selection/segment, routes, process contracts/entry, rating/public/detailed mappers, focused tests | `Get-Content`/`rg` source and test inspection | Current unit is `TrackSegment`; current selection filters rejected segments, selects first composite-quality segment, and child returns JSON response to parent. | Implemented and production-wired | none | Candidate unit, process/rating gate | Alternative plausibility and public result shape unresolved. | Create implementation-discovery document. |
| 2026-08-29T12:49:03.1336623Z | Create implementation discovery | Record source-backed minimal design, tests, risks, and no-go. | `07-dominant-target-implementation-discovery.md` | `apply_patch` | Added proposed implementation-discovery document; no runtime/API/test changes. | Proposed | Sprint 1 documentation | Implementation design | Approval required before implementation. | Static verification. |
| 2026-08-29T12:51:13.4379746Z | Run initial static verification | Check diff, whitespace, links and permitted scope. | New discovery document and required workplan files | `git diff --check`; `rg`; PowerShell link/scope checks | Diff/whitespace clean and scope correct. Link check found new document source/test links used one directory level too few. | Unknown / requires verification | none | Documentation traceability | Correct repository-relative links. | Patch links and rerun checks. |
| 2026-08-29T12:51:13.4379746Z | Correct documentation links | Fix repository-relative source/test links in discovery document. | `07-dominant-target-implementation-discovery.md` | `apply_patch` | Replaced `../../src|tests` links with `../../../src|tests` links. | Proposed | Sprint 1 documentation | Documentation traceability | Recheck links and final scope. | Final static verification. |
| 2026-08-29T12:52:03.8029344Z | Final implementation-discovery verification | Verify diff, whitespace, repository-relative links, scope, status and stat. | Allowed changed discovery documents | `git diff --check`; `rg`; PowerShell link/scope checks; `git status`; `git diff --stat` | Diff check clean; no trailing whitespace; all links resolve; only log, verification, README and new 07 document changed. | Proposed | none | Implementation discovery | Policy/result/API approval remains unresolved. | Human review. |
| 2026-08-29T13:03:45.2879493Z | Verify corrected track-first candidate design | Establish whether `PlayerTrack` rather than `TrackSegment` is the source-backed dominance unit. | `selection.py:PlayerTrack,TargetPlayerSelector,WeightedTargetPlayerSelector`; `player_tracker.py:DetectionOnlyPlayerTracker.analyze,_summary`; `segment_selection.py:TrackSegment,build_segments,rank_segments,select_segment`; `routes.py:_analyze_uploaded`; `analysis_composition.py:create_analysis_components`; `core/config.py:Settings`; `config/football_profiles.py`; `tests/test_selection.py`; ADR-005, contract, 07, log, verification, README | `Get-Content`/`rg`; Git state commands | `PlayerTrack` summarizes tracker observations; default segment mode ranks every eligible segment and cannot reach its generic ambiguity gate. Legacy selector is visibility-only after qualification, with exact strict-margin behavior. Required corrected design is track-first, then selected-track segments only. | Implemented and production-wired / Proposed | none | Track-first dominance design | Track/plausibility/dominance, duplicate-observation, FPS, and public-result policies remain unresolved. | Correct 07 and hierarchy wording; static-verify documentation. |
| 2026-08-29T13:03:45.2879493Z | Correct implementation-discovery design | Replace segment-first proposal with track-first source-backed design. | `07-dominant-target-implementation-discovery.md`; `README.md` | `apply_patch` | 07 now makes `PlayerTrack` the target/dominance candidate and `TrackSegment` selected evidence within its winning track; README hierarchy matches. No runtime/test/API/schema/formula/settings/deployment/dependency/CV/live/commit/push action. | Proposed | `00-discovery-log.md`, `07-dominant-target-implementation-discovery.md`, `README.md` | Candidate semantics | Human approval and concrete policy decisions are still required. | Run final static/scope verification and record outcome. |
| 2026-08-29T13:06:49.6791595Z | Final track-first documentation verification | Validate whitespace, repository-relative links, changed-file scope, Git state, and diff stat after correction. | All Sprint 1 Markdown links; allowed changed files | `git diff --check`; `rg` trailing-whitespace scan; PowerShell link/scope check; `git status`; `git diff --stat` | No diff-check content error, trailing whitespace, broken repository-relative link, or out-of-scope file. The initial scope expression misclassified the allowed `??` new 07 file and was corrected before the successful rerun. Final scope is three modified workplan files plus new 07; tracked stat is 49 insertions/4 deletions and excludes untracked 07. LF-to-CRLF warnings only. | Implemented but not production-wired | `00-discovery-log.md`, `06-verification-results.md` | Documentation verification | Policy/Apex approval remains unresolved; tests intentionally not run. | Human approval of corrected design and test plan. |
| 2026-08-29T13:15:29.0673238Z | Start tests-only phase and inspect test seams | Establish safe test scope, baseline state, and planned pure-contract target. | User brief; `AGENTS.md`; all five handoff files; ADR-005; target-selection contract; all Sprint 1 workplans; `selection.py`; `player_tracker.py`; `segment_selection.py`; rating/public mapper; `test_selection.py`; `test_segment_selection.py`; test-symbol search | `Get-Content`; `rg`; `git branch`; `git rev-parse`; `git status`; `git diff --stat` | Branch/HEAD is `the-new-inhancement` / `275141dâ€¦`; only expected user-owned Sprint 1 documentation changes exist. Existing models/builders support a planned pure test contract, but no dominant-target runtime symbols exist. `PlayerTrack.visible_frames` is unsafe as unique evidence because tracker list observations can duplicate a frame while boxes are keyed by frame. | Implemented and production-wired / Proposed | `00-discovery-log.md` | Tests-only contract | Need establish baseline non-inference tests and then add intentionally failing contract tests without an unapproved public API. | Run relevant baseline tests. |
| 2026-08-29T13:18:15.5944241Z | Create focused dominant-target contract tests and update discovery | Define approved internal behavior before runtime implementation. | `tests/test_dominant_target_selection.py`; `07-dominant-target-implementation-discovery.md` | `apply_patch` | Added 33 deterministic, local-import tests for unique evidence, qualification, plausible alternatives/dominance, winning-track segments, and rating gate. Updated 07 to require unique `player_boxes` frame keys, provisional inclusive 0.08 margin, and no track merging. Expected new-test outcome is import failure until the planned internal module exists; no test is skipped. | Proposed | new test module; `07-dominant-target-implementation-discovery.md` | Test contract | Planned internal symbols are absent by design; public diagnostic/API assertions remain deferred. | Run baseline/new focused tests and available static checks. |
| 2026-08-29T13:18:15.5944241Z | Attempt baseline, focused test, lint, and formatter verification | Verify existing behavior before/after test creation without CV inference. | Existing selection/segment/rating tests; new dominant-target test | `uv run pytest -q <focused>`; `.venv\\Scripts\\python.exe -m pytest -q <focused>`; `ruff check|format --check <new test>` | Neither baseline nor new test reached pytest: UV cache initialization failed (`os error 183`) and the UV trampoline could not spawn Python (`os error 5`). Standalone Ruff and system Python are unavailable. These are unexpected environment/tooling blockers, not test results; expected missing-policy import failures could not be observed. | Unknown / requires verification | none | Test execution | Need a usable Python/UV/Ruff environment to establish baseline and capture expected missing-module failures. | Inspect changed files and complete non-execution static checks. |
| 2026-08-29T13:19:47.0983730Z | Retry focused baseline with temporary cache and elevated interpreter access | Distinguish sandbox startup restrictions from project/test failures without changing repository state. | Existing selection/segment/rating tests; temporary UV cache outside checkout | `UV_CACHE_DIR=<temp>; uv run pytest -q <focused>` | Sandboxed retry failed querying Python (`os error 5`). Approved elevated retry reached editable build but failed `hatchling.build.build_editable`: `ModuleNotFoundError: No module named 'typing'`; the tool suggested build dependency changes. No pytest collection, baseline result, or expected missing-policy import failure occurred. Dependency workaround is out of scope. | Unknown / requires verification | none | Test execution environment | Project build environment is broken independently of the new test contract. | Do not alter dependencies; finish static verification and seek human review. |
| 2026-08-29T13:20:21.2312202Z | Final tests-only static and scope verification | Verify changed test/document content without a usable Python toolchain. | `test_dominant_target_selection.py`; changed Sprint 1 Markdown; Git state | `git diff --check`; trailing-whitespace scan; repository-relative link check; forbidden-scope check; `git status`; `git diff --stat` | No diff-check error, trailing whitespace, broken workplan link, runtime (`src/`), schema, config, or dependency change. Ending status is prior three modified workplan files plus untracked 07 and new test module. Tracked stat is 90 insertions/4 deletions and excludes untracked files. Tests/lint/format remain unexecuted due recorded environment blockers. | Implemented but not production-wired / Unknown / requires verification | `00-discovery-log.md`, `06-verification-results.md` | Final test contract verification | Exact expected missing-policy import failures and existing-test baseline remain unverified. | Human review of test contract and environment blocker. |
| 2026-08-29T13:27:29.8913566Z | Apply human-review scope correction | Remove artificial tracking-result and rating-gate requirements from pure target-selection tests. | Review brief; ADR-005; target-selection contract; 07; verification records; `test_dominant_target_selection.py` | `Get-Content`; `git` inspection; `apply_patch` | Removed the one tracking-count and four rating/lifecycle/event tests; planned test API now limits itself to target status/result, unique evidence, dominance, and winning-track segment selection. Added correct ownership and future integration acceptance plan. Expected test count becomes 28. | Proposed | test module; `00-discovery-log.md`; `07-dominant-target-implementation-discovery.md` | Pure selection contract | Must diagnose interpreter without mutation and static-verify corrected files. | Run required read-only Python/venv/UV diagnostics. |
| 2026-08-29T13:28:37.2722277Z | Diagnose Python/venv/UV without mutation | Identify executable, base-interpreter, standard-library, trampoline, and cache failure layers. | `python`; `.venv\\Scripts\\python.exe`; `.venv\\pyvenv.cfg`; configured UV base path; `uv` | Prescribed `Get-Command`, `where`, Python/typing, venv Python/typing, `Get-Item`, config, and UV commands; read-only `Get-Item` base-path check | No `python` is on PATH. Venv executable exists but its UV trampoline cannot spawn a child (`os error 5`) for either `sys` or `typing`. `pyvenv.cfg` points to CPython 3.12.13 under Roaming UV; sandbox reads of that base path are denied/then report not found, so its files cannot be verified. UV itself exists but its configured cache collides (`os error 183`). `typing` is standard library; no install is appropriate. | Unknown / requires verification | `00-discovery-log.md`; `06-verification-results.md`; `07-dominant-target-implementation-discovery.md` | Environment diagnosis | Classify confirmed blockers as process-creation permissions and UV cache failure; base interpreter availability remains unverified due access denial. | Source-count/inspect 28 tests and run static scope checks only. |
| 2026-08-29T13:29:25.2850121Z | Verify corrected pure-selection contract | Confirm test count, absence of over-scoped assertions, static integrity, links, and permitted scope. | Complete corrected test module; 07; Sprint 1 docs; Git state | `Get-Content`; source `rg '^def test_'`; forbidden-term scan; `git diff --check`; trailing-whitespace/link/scope checks; `git status`; `git diff --stat` | Exactly 28 test functions. No `gate_player_ratings`, tracking-run counter, rating/Overall/callback/event/lifecycle assertion remains in the unit module. No diff-check/trailing-whitespace/link/scope error; no runtime/schema/config/dependency change. Test collection/run stays blocked because direct venv Python cannot start. | Proposed / Unknown / requires verification | `00-discovery-log.md`; `06-verification-results.md` | Corrected test contract | Python base/interpreter access remains unverified; no repair authorized. | Human review of corrected tests and diagnosis. |
| 2026-08-29T13:37:31.3179799Z | Establish environment-repair baseline and launcher inventory | Confirm authorized repair scope, immutable project inputs, compatible interpreter requirement, and current launcher state before mutation. | `AGENTS.md`; handoff; Sprint 1 records; `pyproject.toml`; `uv.lock`; `.venv`; Git state | `git diff --check|status|diff --stat`; `Get-FileHash`; `rg requires-python`; prescribed `Get-Command`/`where`/`py`/`uv`/venv commands | Expected working-tree changes only; diff check clean. SHA-256: `pyproject.toml` `69C5D92Eâ€¦6A80C`, `uv.lock` `5A09F68Fâ€¦CF23`. Project requires Python `>=3.12`. `py.exe` exists but has no installed Pythons; no `python` PATH command. UV 0.11.2 exists but global cache is broken (`os error 183`). Old venv is UV trampoline for CPython 3.12.13. | Implemented and production-wired / Unknown / requires verification | `00-discovery-log.md` | Environment repair | Need choose a verified source; task-specific UV cache avoids global collision. | Create isolated cache after collision-safe path check. |
| 2026-08-29T13:38:02.1988741Z | Create isolated repair cache and inspect existing environment paths | Avoid global cache mutation and verify recoverable backup target before changing `.venv`. | `%TEMP%\\super7-uv-cache-20260829-02`; `.venv`; `.venv.broken-20260829-01` | `Test-Path`; `New-Item -ItemType Directory`; `uv --cache-dir <cache> python list --only-installed` | Cache did not exist and was created; old venv exists; backup path is free. UV still cannot enumerate managed Pythons because its global Python installation directory is access denied (`os error 5`), even with isolated cache. Mutation: created only the named temporary cache; rollback is removing that empty/scoped cache after task approval, not attempted now. | Unknown / requires verification | `00-discovery-log.md`; temp cache | Interpreter source | Need identify UV-supported isolated Python-install-directory syntax without elevation. | Read UV Python-install help. |
| 2026-08-29T13:38:54.6645086Z | Install and verify isolated compatible CPython | Obtain a usable interpreter outside inaccessible global UV paths, using the repository's `>=3.12` requirement and preserved 3.12.13 record. | `%TEMP%\\super7-uv-cache-20260829-02\\python\\cpython-3.12.13-windows-x86_64-none\\python.exe` | `uv python install 3.12.13 --install-dir <cache>\\python --no-bin --no-registry`; direct `sys` and `typing` commands | UV downloaded 20.8 MiB and installed CPython 3.12.13 into the task-specific directory. Direct interpreter starts and imports standard-library `typing.py`. Mutation is confined to the temporary cache; rollback is preserving it for this task then removing that exact directory later if appropriate. | Implemented but not production-wired | `00-discovery-log.md`; temp cache | Interpreter source | Compatible verified interpreter available; preserve broken `.venv` next. | Rename `.venv` to checked backup path. |
| 2026-08-29T13:40:33.0585097Z | Preserve broken virtual environment | Move the non-starting UV-trampoline `.venv` to a reversible backup before replacement. | `E:\\super7\\.venv`; `E:\\super7\\.venv.broken-20260829-01` | Checked source/destination with `Test-Path`; `Move-Item -LiteralPath` | Mutation: `.venv` renamed to `.venv.broken-20260829-01`; source is absent and backup exists. Rollback before a replacement occupies `.venv`: `Move-Item .venv.broken-20260829-01 .venv`. Backup will not be removed. | Implemented but not production-wired | `00-discovery-log.md`; local `.venv` path | Environment preservation | Need create a replacement strictly from frozen lock state using verified CPython and isolated cache. | Run frozen UV sync with explicit interpreter. |
| 2026-08-29T13:41:40.8692379Z | Create and inspect replacement locked environment | Build `.venv` from frozen project state and verify interpreter/dependency availability. | `E:\\super7\\.venv`; isolated CPython/cache | `uv --cache-dir <cache> sync --frozen --python <verified-python>`; direct `sys`, `typing`, `pip`, and `pytest` commands | Mutation: sync created `.venv`, built the local project, and downloaded lock-resolved artifacts, but did not complete within the execution window. Replacement Python starts and imports `typing`; `pip` and `pytest` are currently absent, so the replacement is partial. Rollback is unavailable while `.venv` occupies the target; preserve both environments and rerun the identical frozen sync only. | Unknown / requires verification | `00-discovery-log.md`; `.venv`; temp cache | Dependency installation | Need finish same frozen sync; no alternative package command or lock change authorized. | Rerun identical frozen sync once. |
| 2026-08-29T13:42:56.0969012Z | Complete frozen dependency synchronization | Finish the same lockfile-only sync after the initial execution-window interruption. | `E:\\super7\\.venv`; isolated cache | Repeated identical `uv --cache-dir <cache> sync --frozen --python <verified-python>`; direct `import pytest`; `python -m pip --version` | The continuation produced no additional command output but `pytest 8.4.2` now imports successfully. `pip` remains intentionally absent from the UV-created environment. Mutation remains limited to `.venv` and scoped cache; old backup remains. | Implemented but not production-wired | `00-discovery-log.md`; `.venv`; temp cache | Environment verification | Verify immutable hashes/scope before test collection. | Check hashes and Git scope, then compile/collect. |
| 2026-08-29T13:43:25.0081748Z | Verify replacement and collect contract tests | Confirm locked inputs stayed immutable, syntax is valid, and the focused pure-selection suite remains exactly 28 tests. | `pyproject.toml`; `uv.lock`; `.venv`; new contract test; Git state | `Get-FileHash`; direct `py_compile`; `pytest --collect-only -q` | Both SHA-256 hashes exactly match baseline. New venv collects all 28 tests in 1.88 s; no `.venv`/backup/cache artifact appears in Git status. Mutation: none. Rollback remains old backup preservation; next action is baseline/new test execution. | Implemented but not production-wired | `00-discovery-log.md`; `06-verification-results.md` | Test readiness | Run relevant existing offline baseline separately. | Execute six relevant existing offline test modules. |
| 2026-08-29T13:44:03.4936052Z | Run focused baseline, contract, combined, and test-file static checks | Establish existing behavior and classify new-test failures after environment repair. | Existing selection/segment/tracker/rating/public-contract tests; new 28-test contract file | Direct `pytest -q <six existing>`; `pytest -q <new>`; combined focused command; `python -m ruff check|format --check <new>` | Existing baseline: 42 passed in 2.43 s, with only pytest-cache permission warning. New contract: 28 collected, 28 failed solely with `ModuleNotFoundError: services.dominant_target_selection`, as expected; no skips. Combined: 42 passed/28 expected missing-module failures. Ruff check passed; Ruff format check reports the user-owned test file would be reformatted. Mutation: none; no auto-format per scope. | Implemented but not production-wired / Proposed | `00-discovery-log.md`; `06-verification-results.md` | Test result | Format difference is a deferred test-correction item; expected implementation failures are now confirmed. | Final hash/scope/static verification. |
| 2026-08-29T13:44:40.7726880Z | Final environment-repair immutability and static verification | Prove locked project inputs and permitted scope survived repair. | `pyproject.toml`; `uv.lock`; replacement/backup venv paths; Sprint 1 docs/tests; Git state | `Get-FileHash`; `git diff --check|status|diff --stat`; trailing-whitespace/link/forbidden-scope checks; `Test-Path` | Both hashes remain baseline-identical; diff check, whitespace, and links are clean; no runtime/schema/config/dependency/lockfile changes. Replacement `.venv` and `.venv.broken-20260829-01` both exist and are ignored by Git. Ending Git status retains only expected Sprint 1 docs/test changes. Mutation: none; rollback backup is preserved, but should not be moved while replacement exists. | Implemented but not production-wired | `00-discovery-log.md`; `06-verification-results.md` | Final repair verification | Test-file formatting remains deferred; minimal runtime implementation still requires review. | Human review of repaired environment and observed baseline. |
| 2026-08-30T15:22:02.0749458Z | Start isolated dominant-target module TDD step | Confirm expected starting state, format approved test contract, and capture red result before runtime implementation. | Task brief; AGENTS/handoff; ADR-005; target-selection contract; 07; test contract; `selection.py`; `segment_selection.py`; Git state | `ruff format tests/test_dominant_target_selection.py`; source test count; `pytest -q tests/test_dominant_target_selection.py` | Formatting-only test update retained 28 functions. Red phase is 28 failures, all `ModuleNotFoundError: services.dominant_target_selection`; no behavioral test change or unrelated failure. | Proposed | test module; `00-discovery-log.md` | Isolated TDD implementation | Existing types/settings can express the contract; no pipeline change is needed. | Add only the approved pure service module. |
| 2026-08-30T15:23:31.7605994Z | Implement and format pure track-first selector | Satisfy the approved unit contract without production wiring. | `selection.py:PlayerTrack`; `segment_selection.py:TrackSegment,rank_segments`; formatted contract tests | `apply_patch` added `src/services/dominant_target_selection.py`; direct pytest/Ruff/py_compile/import commands; `ruff format` | Added only immutable status/evidence/result values plus unique evidence, qualification/dominance, and winning-track segment selection. It uses unique box-frame keys, existing thresholds/margin, deterministic `track_id` tie order, and existing segment ranking. Initial formatter found only new-source layout; after format, 28 tests pass, Ruff check passes, both changed Python files are formatted, compile/import pass. No route/composition/API/rating/configuration change. | Implemented but not production-wired | new module; formatted test; `00-discovery-log.md` | Isolated selection behavior | No mismatch found between tests, ADR, contract, and existing types for this pure scope. | Run existing baseline and combined focused suite. |
| 2026-08-30T15:23:47.7627283Z | Regress isolated implementation | Confirm existing behavior and combined contract compatibility without inference. | Existing six focused offline modules; new dominant-target module/test | `pytest -q <six baseline modules>`; combined command with new contract test | Existing baseline is 42 passed in 2.31 s; combined set is 70 passed in 1.86 s. Both emit only pytest-cache write permission warning. No model/CV/live operation. | Implemented but not production-wired | `00-discovery-log.md`; `06-verification-results.md`; `07-dominant-target-implementation-discovery.md`; `README.md` | Isolated implementation verification | Pipeline integration, target-dependent rating gate, and public/API/callback decisions remain deferred. | Final scope/static verification and human review. |
| 2026-08-30T15:26:11.9946232Z | Final isolated-module scope/static verification | Confirm the completed pure-module step has no unintended change or documentation defect. | New module/test; changed Sprint 1 workplans; Git state; `pyproject.toml`; `uv.lock` | `git diff --check`; trailing-whitespace scan; Git status/stat; SHA-256 check | Diff and whitespace are clean. The first generic link-check attempt was invalid because it resolved document-relative links from the repository root; it made no change and is not treated as a documentation failure. Scope is exactly the intended Sprint 1 records plus the new pure module and approved test. `pyproject.toml` and `uv.lock` retain their recorded hashes. | Implemented but not production-wired | `00-discovery-log.md`; `06-verification-results.md` | Isolated implementation verification | Only pipeline integration, rating gate, and public/API/callback decisions are deferred. | Human review; do not wire this module into production pipeline without a separately approved integration contract. |
| 2026-08-30T15:51:03.2508224Z | Inspect tests-only integration boundary and stop on missing seam | Determine whether the reviewed pure selector can be contract-tested through current orchestration without runtime or schema changes. | New task brief; all mandatory handoff/contract/ADR/discovery/module/test inputs; `routes.py:_analyze_uploaded,_completed`; `PlayerRatingEngine`; public mapper; schemas; existing route/rating/process tests; Git state | `Get-Content`; `rg`; `git branch --show-current`; `git rev-parse HEAD`; `git status --short`; `git diff --stat` | Branch/HEAD match the brief and starting changes are the expected reviewed documentation, pure module, and unit contract. `_analyze_uploaded` is segment-first, imports no dominant-target module/result, and has no target-status or availability carrier. `CompletedResponse` requires a selected player and public response/callback changes are pending Apex; `PlayerRatingEngine` currently permits Physical + Ball Involvement Overall without Technical. A faithful 6â€“12 test contract would therefore require prohibited production wiring or schema/formula changes. No test was added, formatted, or run. | Unknown / requires verification | `00-discovery-log.md`; `06-verification-results.md`; `07-dominant-target-implementation-discovery.md`; `README.md` | Integration-contract feasibility | Missing internal orchestration gate/result seam; unavailable-rating projection and proposed Overall eligibility have no current non-public representation. | Stop for human review; approve a minimal internal result/gate seam before integration tests or runtime wiring. |
| 2026-08-30T16:03:19.1944325Z | Reassess integration-tests-only task after resolver approval | Apply the human-approved single-resolver seam and verify whether all required test outcomes are now representable without schema change. | Continuation brief; `AGENTS.md`; contracts/ADR/discovery; selector/unit test; `routes.py:_analyze_uploaded,_completed`; `public_rating_mapper.py`; `analysis.py`; existing route/rating/process tests; Git state | `Get-Content`; `rg`; Git inspection | The approved future `resolve_dominant_target` composition helper is sufficient for tracking-once and established/no-fallback tests, with no DI framework. However it does not solve the explicit stop condition: `CompletedResponse` requires `SelectedPlayer`, and `NonCompletedResponse` has neither player ratings nor Overall/confidence fields. Thus existing response variants cannot express a successful target-unavailable result with all ratings/Overall null using existing fields. A test asserting that behavior would require a prohibited public schema/mapper decision; no new test, runtime code, or verification suite was run. | Unknown / requires verification | `00-discovery-log.md`; `06-verification-results.md`; `07-dominant-target-implementation-discovery.md`; `README.md` | Integration-contract feasibility | Public/internal representation of target-unavailable successful analysis remains absent even after resolver approval. | Stop for human review; approve the result-projection representation before creating the requested integration suite. |
| 2026-08-30T20:08:21.3825553Z | Record Apex-confirmed response contract and inspect public surfaces | Apply the explicit Apex semantic decision and determine whether exact public-schema tests can target one current surface without implementation. | Continuation brief; AGENTS/handoff; target contract; ADR-005; all Sprint 1 workplans; `public_rating_v2.py`; `callback_service.py`; `public_rating_mapper.py`; `routes.py:_callback_payload`; schema/callback tests; Git state | `Get-Content`; `rg`; Git inspection | Starting branch is `the-new-inhancement` at committed Sprint 1 HEAD `205c80eâ€¦`, with clean worktree. Contract fields/reasons are confirmed, but current callback payload has top-level `status` and strips `player`; Public Rating V2 retains `player`/`overall` but nests status and lacks `resultAvailability`, `unavailabilityReason`, and `overallConfidence`. Exact confirmed JSON cannot be asserted against one existing public surface without independently choosing a compatibility mapping. Documentation updated; no test added or run under the explicit callback-envelope stop condition. | Contract confirmed / Schema not implemented / Mapper not implemented / Pipeline gate not implemented / Not deployed | contract, ADR, Sprint 1 documentation | Public response compatibility | Placement/versioning choice between callback and Public Rating V2 remains unapproved. | Human review must approve the compatibility surface before tests or implementation. |
| 2026-08-30T20:17:02.1930686Z | Add final CallbackPayload availability contract tests | Apply final human surface choice without implementing schema or mapping. | Final brief; `CallbackPayload`; V2 player type; `_callback_payload`; rating summary/engine; existing callback/schema tests | `apply_patch`; Ruff format; pytest collection/new/existing/combined; Ruff check/format; `py_compile` | Canonical Apex surface is additive `CallbackPayload`; V2 player dict is reused and internal source is `PlayerRatingSummary.overall.confidence`. Added six tests. Collection succeeds. New suite is 6 expected reds: four missing `resultAvailability` serialization failures and two absent invariant-validation failures; no fixture/import/environment failure. Existing callback/schema 16, selector 28, and offline baseline 42 all pass; combined is 70 pass plus 6 expected red. | Contract confirmed / Schema not implemented / Mapper not implemented / Pipeline gate not implemented / Not deployed | new callback contract test; governing/docs records | Callback availability contract | Runtime schema/mapping/resolver/pipeline remain intentionally absent. | Static/diff/link/scope verification; human review before green implementation. |
| 2026-08-30T20:22:40.1686989Z | Implement CallbackPayload green phase | Add only the approved schema fields/invariants and completed-result callback mapping. | Red contract; `callback_service.py:CallbackPayload`; `routes.py:_callback_payload`; V2 player projection; rating summary/engine; all construction-site search results; focused tests | Red pytest; `apply_patch`; Ruff format; focused pytest; Ruff/py_compile/import checks | Added optional aliased availability fields so existing noncompleted `CallbackPayload` construction remains unlabelled, while explicit AVAILABLE/UNAVAILABLE values enforce approved state invariants. `_callback_payload` now maps `CompletedResponse` to uppercase `COMPLETED`, `AVAILABLE`, the unchanged V2 player dict, and exact `PlayerRatingSummary.overall.confidence`. Unavailable schema construction is supported but pipeline production wiring is not added. Six contract tests pass; callback/schema 16, selector 28, baseline 42, combined 76 all pass. | Implemented but not production-wired | `callback_service.py`; `routes.py`; callback mapper test; workplan records | Callback availability schema/mapping | Target-unavailable resolver/gate and production emission remain deferred. | Final static/scope verification and human review. |
| 2026-08-30T20:29:25.7226302Z | Define internal successful-unavailable result contract | Identify the unique child/parent carrier and add red tests without internal-schema or mapper implementation. | `_analyze_uploaded`; `CompletedResponse`, `NonCompletedResponse`, `AnalyzeResponse`; child entrypoint/contracts/pool; callback mapper/schema; existing process/rating tests | `Get-Content`; `rg`; `apply_patch`; pytest collection/new/preserved/combined; Ruff/py_compile | Authoritative successful carrier is `CompletedResponse`: producer `_analyze_uploaded` â†’ child `model_dump_json` in `ChildAnalysisSuccess.response_json` â†’ `AnalyzeResponse` validation in `validate_child_result` â†’ `_callback_payload` â†’ `CallbackPayload`. Added five tests. One available preservation test passes; four expected red failures are only `CompletedResponse` rejecting null `selected_player` and `scores`. Callback availability 6, callback/schema 16, selector 28, baseline 42 remain green; combined 77 pass plus four expected red. No runtime code changed. | Proposed / Implemented but not production-wired | new internal-result contract test; Sprint 1 records | Internal carrier decision | Green phase must extend/restructure `CompletedResponse` or an approved compatible successful-result representation, then map unavailable result; resolver/pipeline stay deferred. | Static/scope verification and human review. |
| 2026-08-31T12:28:51.8000388Z | Implement internal successful-unavailable green phase | Add compatible carrier validation, shared vocabulary, callback projection, and process round-trip coverage without production emission. | Approved red contract; `CompletedResponse`/`AnalyzeResponse`; `CallbackPayload`; `_callback_payload`; child contracts | `rg`; `apply_patch`; direct pytest; Ruff; mypy; `py_compile`; Git/static checks | Availability/reason literals moved from callback ownership to `schemas.analysis`, yielding one neutral vocabulary without a cycle. Legacy/explicit AVAILABLE requires player and scores with no reason; explicit UNAVAILABLE requires null player/scores/rating summary and an approved reason. The mapper returns a completed unavailable payload with null player/Overall/confidence and empty unavailable-compatible maps. The five-test contract now includes the child JSON/parent validation round-trip. `_analyze_uploaded` remains unchanged and no resolver/pipeline emission is added. | Implemented but not production-wired | `analysis.py`; `callback_service.py`; `routes.py`; internal contract test; Sprint 1 records | Internal successful-unavailable carrier | Default OS pytest temp access is denied; parent/child tests pass with an isolated workspace-local `--basetemp`. The existing `.pytest_cache` warning is non-behavioral. | Final documentation, links, diff, and scope verification; resolver integration remains separate. |
| 2026-08-31T12:28:51.8000388Z | Add resolver-integration route red contract | Specify approved future orchestration without adding production wiring. | Human approval; ADR/target contract; `dominant_target_selection`; `_analyze_uploaded`; current green carrier; route/selection tests | `Get-Content`; `rg`; `apply_patch`; direct pytest; Ruff | Added four red cases for the documented `resolve_dominant_target(run, *, fps, settings)` seam. Established target must consume the one tracker result and continue through the exact resolver segment; all three unavailable reasons must return successful-unavailable `CompletedResponse` without `_completed`. Current route fails only because it remains segment-first, never invokes the resolver, and enters `_completed` for unavailable targets. Existing selector/internal-carrier coverage is 33/33 green. | Proposed | new route-contract test; Sprint 1 records | Resolver integration contract | Green implementation is explicitly blocked pending review. | Human review of the red contract; do not wire production behavior. |
| 2026-08-31T12:58:50.4070594Z | Repair public mapper optional-field narrowing | Remove the accepted carrier's mypy regression without touching resolver/pipeline behavior. | Hotfix brief; `CompletedResponse` validator; public mapper/callers; mapper/internal/callback/route tests; Sprint 1 records | `mypy src/api/public_rating_mapper.py`; `apply_patch`; direct pytest; Ruff; syntax/import; Git checks | Initial mypy reported 14 nullable player/scores dereferences. Added explicit unavailable rejection and non-null available guards in the mapper plus helper guards where the values are consumed. Added one mapper regression test that blocks unavailable V2 projection. Post-change affected-file mypy passes; green suites pass; route contract remains four expected red failures because no resolver/pipeline wiring changed. | Implemented but not production-wired | `public_rating_mapper.py`; `test_player_rating_wiring.py`; Sprint 1 records | Mapper type safety | No casts, ignores, defaults, formulas, API/schema/config/dependency change, or resolver behavior change. | Approved resolver green integration remains the next separate decision. |
| 2026-08-31T16:20:00+03:00 | Audit dominant-target resolver green wiring | Classify the complete runtime diff, verify the new route gate, and record missing verification. | `AGENTS.md`; handoff; runtime diff; route/resolver/carrier/callback/process tests; Sprint 1 records | Git normal/`-w` diff; source inspection; focused pytest; source-root mypy; Ruff; compile/import; static checks | The former global segment-first branch is wholly replaced by one resolver over the one existing `TrackingRun`. No second tracker/detection/video scan, legacy fallback, cross-track segment fallback, or later re-ranking exists. The unavailable builder is required because `_completed` performs target-attributed analysis. No incidental runtime hunk was found or removed. The historical 42-test baseline is recorded only by count, not command; a reconstructed candidate encountered Windows Temp/OpenCV AVI-writer environment limits and is not baseline evidence. | Implemented and production-wired; offline fixture verification remains required | `routes.py`; `dominant_target_selection.py`; `06-verification-results.md`; `07-dominant-target-implementation-discovery.md`; `README.md` | Resolver integration audit | `.vscode/settings.json` is unrelated and untouched. Workspace-local pytest temp makes process tests pass; cache warning is non-behavioral. | Human review plus known historical baseline command and codec-capable offline fixture environment; no deployment/commit/push. |
| 2026-08-31T16:45:00+03:00 | Final baseline parity and offline fixture validation | Compare the prior AVI fixture failure at clean HEAD and active resolver checkout without changing either runtime tree. | Detached HEAD worktree; identical `.venv`; `PYTHONPATH` import proof; video validator/path tests; resolver/carrier/callback/process suites | `git worktree add --detach`; direct `python -m pytest --basetemp`; source-root mypy; Ruff; compile/import; static checks | Clean HEAD and active checkout each imported `api` and `dominant_target_selection` from their own `src` roots and both passed `test_validator_extracts_metadata_from_decodable_video`. The earlier MJPG writer failure is non-reproducible, not a resolver regression or persistent environment limitation. The permanent reconstructed offline candidate command is recorded in verification results; it now gives 41 passed and one expected Windows symlink-permission skip. | Implemented and production-wired; reconstructed candidate is not historical baseline certification | `06-verification-results.md`; `07-dominant-target-implementation-discovery.md`; `README.md` | Baseline parity | No test or runtime change; `.vscode` untouched; no real production video, model download, network callback, deployment, commit, or push. | Safe for local commit review; controlled offline video validation remains separate from deployment. |
| 2026-08-31T00:00:00+03:00 | Diagnose reproducible completed-to-cancelled lifecycle failure | Classify the full-suite blocker without changing runtime behavior. | Failing integration node; `AnalysisWorker`; `AnalysisQueue`; parent process callback path; adjacent worker-shutdown tests; clean detached HEAD | Active and clean isolated pytest loops; file/package/full pytest; explicit clean `PYTHONPATH` import proof; `git worktree add/remove` | Active and clean HEAD each failed the exact node 10/10. Active file/package/full results were 6/1, 24/1, and 388/1/1 Windows symlink skip; clean file/full were 6/1 and 388/1/1 skip. Logs consistently show completed callback/execution, then shutdown cancelling one active worker and terminalizing `CANCELLED`. | Empirically observed pre-existing lifecycle defect | `00-discovery-log.md`; `06-verification-results.md` | Lifecycle terminal-state semantics | The test waits for callback insertion, not worker terminal completion; shutdown cancels the active task before it returns `COMPLETED`. No fix was made. | Separate lifecycle repair and regression-test review; do not push typing repair while CI parity fails. |
| 2026-08-31T00:00:00+03:00 | Implement bounded active-analysis shutdown grace | Correct deterministic active-job cancellation after completed callback while preserving bounded cancellation. | Approved product decision; `Settings`; `main.py`; `AnalysisWorker`; focused queue/config/integration/process tests | `apply_patch`; repeated isolated pytest; full pytest; mypy; Ruff | Added `analysis_shutdown_grace_seconds=5.0`, finite-positive validated and environment parsed. Lifespan passes it to the worker. Shutdown stops admission/cancels queued work, waits only for the one active processor to terminalize, then cancels after grace; idle worker stops immediately and no next queued job starts. | Implemented and production-wired | lifecycle runtime/tests; Sprint records; `.gitignore` hygiene | Grace ownership and terminal-state semantics | Five seconds is the explicit approved bounded operational grace, not `request_deadline_seconds`. `RequestLifecycle.shutdown()` and pool shutdown remain unbounded deferred limitations. | Review local diff; do not push until reviewed CI-repair commit. |
